# Hongshan Health IRMI-Core 项目概述

## 项目简介

Hongshan Health IRMI-Core 是一个医保智能审核核心类库，全称为 Intelligent Review of Medical Insurance。该项目旨在提供一套完整的医保费用审核系统，能够检测各种医保违规行为，如重复收费、超标准收费、超医保支付范围、不合理诊疗等。

## 项目架构

### 核心组件

1. **IRMI (医保智能审核管理类)** - 项目的主要入口点，负责管理不同的驱动实例
2. **Driver (驱动基类)** - 定义驱动的基本行为，目前包含陕西医保智能审核驱动
3. **处理器系统** - 根据不同类型的规则执行相应的审核逻辑
4. **结构体系统** - 定义数据模型，如规则、病历等
5. **工具类系统** - 提供各种实用功能

### 目录结构

```
src/
├── config/           # 配置文件
├── constant/         # 常量定义
├── driver/           # 驱动实现
├── interfaces/       # 接口定义
├── processor/        # 处理器实现
│   ├── emr/          # 电子病历处理器
│   └── insurance/    # 医保项目处理器
├── rule/             # 规则相关
├── struct/           # 数据结构定义
└── Driver.php        # 驱动基类
└── IRMI.php          # 主管理类
└── IRMIException.php # 异常类
└── IRMILog.php       # 日志类
└── Util.php          # 工具类
```

## 核心功能

### 1. 规则管理 (IRMIRuleSet)

- 支持定义医保智能审核规则集合
- 提供基于项目编码的规则过滤功能
- 支持黑白名单机制
- 包含字典数据支持

### 2. 规则定义 (IRMIRule)

支持多种规则类型：
- 重复收费 (type=1)
- 超标准收费 (type=2)
- 超医保支付范围 (type=3)
- 不合理诊疗 (type=4)
- 以及其他自定义类型

### 3. 病历处理 (MedicalRecord)

- 包含患者基本信息（性别、年龄、体重等）
- 诊断和手术信息
- 医保项目集合
- 医院和就诊信息

### 4. 处理器系统

#### 医保相关处理器 (insurance)
- **DuplicateCharge**: 重复收费检测
- **OverStandardCharge**: 超标准收费检测
- **OverInsuranceCharge**: 超医保支付范围检测
- **UnReasonableTreatment**: 不合理诊疗检测

#### 病历相关处理器 (emr)
- **MedicalRecord**: 病历属性合规性检测

## 设计特点

### 1. 插件化架构
- 通过驱动系统支持不同地区的医保政策
- 目前已有陕西医保智能审核驱动

### 2. 规则驱动
- 所有审核逻辑基于规则配置
- 支持复杂的条件组合和嵌套规则

### 3. 灵活的匹配机制
- 支持多种操作符（=, !=, <, >, in, between, regex等）
- 支持字典匹配和公式计算

### 4. 完善的日志系统
- 集成了PSR-3兼容的日志接口
- 提供多种级别的日志记录

## 关键技术实现

### 1. 规则匹配引擎
```php
Util::detectFormula($props, $formulas, $dict)
```
该方法实现了复杂的公式匹配逻辑，支持条件组合、字典查找等功能。

### 2. 数据结构设计
- **JsonTable**: 统一的结果数据格式
- **Base**: 所有结构体的基类，提供通用的数据加载和转换功能

### 3. 异常处理
- 统一的异常处理机制
- 支持携带额外数据的异常对象

## 使用场景

1. **医保费用审核**: 自动检测医疗机构的医保收费合规性
2. **智能监管**: 帮助医保管理部门识别潜在的违规行为
3. **合规检查**: 协助医院自查自纠，提高收费合规性

## 扩展性

项目采用高度模块化设计，易于扩展：
- 新增医保政策可通过继承Driver类实现
- 新增审核规则类型可通过新增处理器实现
- 新增数据结构可通过继承Base类实现

## 总结

Hongshan Health IRMI-Core 是一个设计良好的医保智能审核系统，具有清晰的架构、灵活的配置和强大的扩展能力，能够满足不同地区医保政策的审核需求。
</contents>